<div class="navbar bg-dark">
    <div class="main bg-dark">
        <% if ( locals.isLoggedIn == true) { %>
            <a href="/login">Logout</a> 
        <% }  else { %>
        <a href="/login">Login</a> <a href="/signup">Registrar</a>
        <% } %>
    </div>
</div>
