extends layout.pug 

block content
    each cat in data
        div(class="box") 
            div(class="forum-category") #{cat.category.title} 
            each forum in cat.forum
                div(class="forums")
                    div(class="forum-item  forum-icon far fa-file") 
                    - url = "forum/"+ forum.forum_id
                    div(class="forum-item")
                        a(href=url)
                            b #{forum.name} 
                            br
                            span(class="forums-description") #{forum.description}
                            br
                    div(class="forum-item") Topics | Posts
                    div(class="forum-item") Último Post
