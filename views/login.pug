extends layout.pug 

block content
    div(class="box")
        h3 Login
        form(action="", method="post" class="form-1") 
            label(for="") Username 
            input(type="text" name="username")
            label(for="") Password 
            input(type="password" name="password")
            input(type="submit")